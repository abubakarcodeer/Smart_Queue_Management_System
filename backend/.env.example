PORT=4000
CLIENT_ORIGIN=http://localhost:8080
MONGO_URI=mongodb://localhost:27017/smart_queue
JWT_SECRET=replace_with_long_random_string
JWT_EXPIRES_IN=7d

# Email (optional - leave blank to disable email sending; in-app notifications still work)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Smart Queue <noreply@smartqueue.local>"
